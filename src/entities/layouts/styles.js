import styled from "styled-components";

export const PageBaseWrapper = styled.div`
    width: 100%:
    height: auto;
    min-height: 700px;

    padding: 100px 150px;
    box-sizing: border-box;
`